apiVersion: v1
kind: Service
metadata:
  name: recommender-service
  labels:
    app: recommender-api
spec:
  type: ClusterIP
  selector:
    app: recommender-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
